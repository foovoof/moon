# Workflow definition: Reporting
id: reporting
name: "Reporting"
version: "0.1.0"
steps:
  - id: step_1
    name: "TODO: define first step of reporting"
    agent: null
    inputs: {}
    outputs: {}
