# Workflow definition: Investigation
id: investigation
name: "Investigation"
version: "0.1.0"
steps:
  - id: step_1
    name: "TODO: define first step of investigation"
    agent: null
    inputs: {}
    outputs: {}
