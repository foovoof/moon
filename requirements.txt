# ==============================================================================
# Moon — قائمة اعتماديات مبسّطة للتشغيل السريع (pip install -r requirements.txt)
# المصدر الرسمي للاعتماديات هو pyproject.toml — هذا الملف مُولَّد يدويًا
# للتوافق مع أدوات/بيئات لا تدعم pyproject.toml (مثل بعض CI قديمة).
# لمزامنة تلقائية: pip-compile pyproject.toml -o requirements.txt
# ==============================================================================

pydantic>=2.13.4,<3
fastapi>=0.139.2,<1
uvicorn[standard]>=0.51.0,<1
httpx>=0.27,<1
networkx>=3.3,<4
PyYAML>=6.0.3,<7
python-dotenv>=1.2.2,<2
structlog>=26.1.0,<27
tenacity>=8.3,<9

# --- Dev / Test (اختياري، يقابل [project.optional-dependencies].dev) ---
pytest>=9.1.1,<10
pytest-cov>=5.0,<6
pytest-asyncio>=1.4.0,<2
ruff>=0.15.21,<1
black>=24.4,<25
mypy>=1.10,<2
import-linter>=2.0,<3
pre-commit>=3.7,<4
